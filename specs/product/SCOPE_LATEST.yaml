# Product scope — post 1.6.2
version_shipped:
  - "1.6.1" # R1 stabilize
  - "1.6.2" # core polish (strategies/rules/display/panels)
version_target: "1.7.0"
in_scope:
  R2_reach:
    - Chrome/Edge packaging verification beyond build target
    - Service worker idle / re-injection smoke on Chromium
    - Store listing prep (assets, privacy, permissions review)
    - README badges / listing copy when ready
  R3_differentiator:
    - ONE of: Chrome Translator/Language Detector API engine OR standalone translate panel
  optional_polish_backlog:
    - empty-result toast when fallback selector engages
    - quick-action secondary "copy translation"
    - pixel-level strategy error chrome unify
out_of_scope:
  - full popup/options redesign (shipped 1.6.0 + polished 1.6.2)
  - PDF translation
  - multi-engine sprawl without demand
  - plans/archive indigo MD3 reimplementation
open_decisions:
  - R3 pick: native-api vs standalone-panel (decide after R2)
  - Whether to ship AMO 1.6.2 listing bump before Chromium store work
